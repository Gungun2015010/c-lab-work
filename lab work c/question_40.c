#include <stdio.h>
//Write a C program to create a Structure named distance having feet and inch as its two elements. Then create two structure variables d1 and d2 and add d1 and d2 by using call by reference.

int main(){
    return 0;
}